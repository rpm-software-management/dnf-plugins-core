=================
DNF noroot Plugin
=================

The noroot plugin will check if root access is needed for the given DNF command and abort with a message if the command is not running under the root UID.
